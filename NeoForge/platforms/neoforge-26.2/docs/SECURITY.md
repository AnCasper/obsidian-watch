# Security Notes

- Keep API keys private.
- Rotate API keys after staff changes or suspected exposure.
- Keep permanent bans disabled until the server has tested config sync, exemptions, cooldowns, appeals, and disconnect messages.
- Use `BAN_DRY_RUN` before enabling real bans.
- Use watchlist `NOTIFY` as the safe default.
- Review website audit logs and mod action logs regularly.
